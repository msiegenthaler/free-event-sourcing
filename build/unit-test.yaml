---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: linkyard/docker-sbt
    tag: 13.13-8u111

inputs:
- name: source-repo

outputs:
- name: test-results

run:
  path: /bin/sh
  args:
  - -c
  - sbt -batch test && mv target/* ../test-results
  dir: source-repo